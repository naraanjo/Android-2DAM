<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme Material3 -->
    <style name="Base.Theme.Gestor" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/background_light</item>
        <item name="android:windowBackground">@color/background_light</item>
    </style>

    <style name="Theme.Gestor" parent="Base.Theme.Gestor">

        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background_light</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/icono_app</item>
        <item name="postSplashScreenTheme">@style/Theme.Gestor</item>
    </style>

</resources>
